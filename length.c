#include "main.h"

int length(const char* str)
{
	return strlen(str);
}
int main()
{
	const char* mystring = _printf(const char *format);
	int count = length(mystring);
	_printf("%d\n", count);

	return 0;
}
